{
  "name": "Uniteams",
  "version": "1.0.0",
  "description": "Uniteams Nuxt.js project",
  "author": "Uniteams",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint",
    "ApiRun": "python ../api/manage.py makemigrations && python ../api/manage.py migrate && python ../api/manage.py runserver localhost:8080",
    "ApiInstall": "pip3 install -r ../requirements.txt"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "bootstrap-vue": "^2.0.4",
    "@nuxtjs/axios": "^5.6.0"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "eslint": "^4.19.1",
    "eslint-friendly-formatter": "^4.0.1",
    "eslint-loader": "^2.1.1",
    "eslint-plugin-vue": "^4.0.0"
  }
}
